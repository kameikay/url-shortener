version: "2"
sql:
  - schema: "sql/migrations/"
    queries: "sql/queries/"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "internal/infra/db/"
        emit_db_tags: true
        emit_json_tags: true

